import React from 'react';
import { ProjectsSlider } from './Carousel'

export const Projects = () => (
  <div className="App-portfolio">
    <h2>Portfolio</h2>
    <p>
      Portfolio here. -Local weather app, -Random quote generator, -Calculator, -expressapis, d3 stuff,
    </p>
    <ProjectsSlider />
  </div>
)
